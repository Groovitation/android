## Goal

Track the app-visible `#26` Texas address validation reland with the required Android companion branch workflow.

## Scope

1. Start from current `origin/main`.
2. Apply only the Android version bump required for this companion flow.
3. Let Android CI validate the branch, then merge to `android/main` while the core branch CI is still running.
